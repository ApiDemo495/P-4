[pytest]
testpaths = backend/tests
asyncio_mode = auto
# The live CycleManager fixture owns asyncio tasks/queues, so tests and
# fixtures must share one event loop for the whole module.
asyncio_default_fixture_loop_scope = module
asyncio_default_test_loop_scope = module
filterwarnings =
    ignore::DeprecationWarning
