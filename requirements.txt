# =============================================================================
# DROSOPHILA TRADER v2.0 - required runtime
#
#   python -m pip install -r requirements.txt
#
# Everything below is needed to run the full engine (22 formulas, signal lock,
# news engine, brain, agents, API + dashboard).  The optional integrations at
# the bottom of the file add real model inference / connectome access; the
# application detects their absence at runtime and degrades gracefully
# (Section 12), it never crashes because an optional package is missing.
# =============================================================================

fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
websockets>=12.0
httpx>=0.27
numpy>=1.26
scipy>=1.11
feedparser>=6.0.11
python-multipart>=0.0.9
redis>=5.0
ntplib>=0.4
msgpack>=1.0
pydantic>=2.7
python-dotenv>=1.0

# -----------------------------------------------------------------------------
# Optional - uncomment / install as needed
# -----------------------------------------------------------------------------
# Local GGUF inference (llama.cpp bindings). Needs a C++ toolchain:
#   pip install llama-cpp-python
# llama-cpp-python>=0.2.79

# Local ONNX inference (prebuilt wheels for most platforms):
#   pip install onnxruntime
# onnxruntime>=1.18

# Live Drosophila connectome (Option A of Section 4.1). Without these the
# engine uses the committed 80x80 fallback matrix:
#   pip install neuprint-python caveclient
# neuprint-python>=0.4.14
# caveclient>=6.2

# -----------------------------------------------------------------------------
# Development / test
# -----------------------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
